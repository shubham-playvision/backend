# T-3: POST /auth/login: verify, issue session, neutral failures

Checkpoint by the TARS TDD orchestration for requirement REQ-42 (Login screen with first-party email/password auth).
Run: tdd-REQ-42-stub-check
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-6 — Valid email+password returns 200, sets the session cookie, and GET /auth/me then returns the user's id and email
- AC-7 — Wrong password, unknown email, and NULL password_hash all return 401 AUTH_INVALID with a byte-identical body and within 20ms of each other (dummy hash verify on the miss paths)
- AC-8 — Requests without the auth_v2 flag enabled return 404 so the surface is invisible until rollout
