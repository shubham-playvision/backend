# T-13: Reset confirm endpoint

Checkpoint by the TARS TDD orchestration for requirement REQ-42 (Login screen with first-party email/password auth).
Run: tdd-REQ-42-stub-check
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-32 — POST /auth/reset-confirm with a valid token and a password of 8+ chars sets the new hash, consumes the token, revokes all existing sessions for the user, and returns 200
- AC-33 — An expired, consumed, or unknown token returns 400 AUTH_RESET_INVALID
