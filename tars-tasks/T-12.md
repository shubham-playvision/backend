# T-12: Reset tokens: table and request endpoint

Checkpoint by the TARS TDD orchestration for requirement REQ-42 (Login screen with first-party email/password auth).
Run: tdd-REQ-42-login-demo
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-30 — POST /auth/reset-request always returns 202 with the same body whether or not the email exists; when it exists, a single-use token (30m TTL, stored hashed) is created and the reset email enqueued
- AC-31 — A second request within 5 minutes for the same email returns 202 without enqueueing another email
