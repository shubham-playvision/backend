# T-4: POST /auth/logout and GET /auth/me

Checkpoint by the TARS TDD orchestration for requirement REQ-42 (Login screen with first-party email/password auth).
Run: tdd-REQ-42-login-demo
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-9 — POST /auth/logout revokes the session, clears the cookie, and returns 204; a replayed old cookie gets 401 on /auth/me
- AC-10 — GET /auth/me without a valid session returns 401 with the standard envelope, never a redirect
