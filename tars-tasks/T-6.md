# T-6: Per-account limiter on auth routes, with a recovery message

Checkpoint by the TARS TDD orchestration for requirement REQ-1 (First-party sign-in for the app).
Run: tdd-first-party-sign-in-for-the-app-layout-check2
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-7 — The 6th attempt within 15m returns 429 with retry_after
- AC-11 — The 429 body names the recovery path and the time the block lifts
