# T-1b: Session store interface

Checkpoint by the TARS TDD orchestration for requirement REQ-1 (First-party sign-in for the app).
Run: tdd-first-party-sign-in-for-the-app-e2e-smoke-1
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-5 — issue(userId) returns a session token that read(token) resolves until expiry
