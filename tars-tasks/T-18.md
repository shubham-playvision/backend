# T-18: Server-side auth metrics and alerts

Checkpoint by the TARS TDD orchestration for requirement REQ-42 (Login screen with first-party email/password auth).
Run: tdd-REQ-42-stub-check
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-43 — Counters for login_success, login_invalid, login_rate_limited, and login_locked emit through the existing metrics client, labeled by error_code only — never by email or IP
- AC-44 — An alert rule fires when the login failure ratio exceeds 30% over 15 minutes, wired into the existing on-call route
