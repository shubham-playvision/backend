# T-2: Sign-in endpoint, constant-time lookup, session issue

Checkpoint by the TARS TDD orchestration for requirement REQ-1 (First-party sign-in for the app).
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-2 — POST /auth/signin with valid credentials returns 200 and a session cookie; GET /me then returns 200
- AC-3 — Wrong password and unknown email both return 401 AUTH_INVALID with a byte-identical body
