# T-10: Account lockout with automatic unlock

Checkpoint by the TARS TDD orchestration for requirement REQ-42 (Login screen with first-party email/password auth).
Run: tdd-REQ-42-stub-check
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-25 — 10 failed attempts within an hour sets locked_until = now + 30m; login during the lock returns 423 AUTH_LOCKED with unlocks_at and the reset-password recovery path named
- AC-26 — After locked_until passes, the next valid login succeeds and clears failed_login_count
- AC-27 — A correct password during the lock still returns 423 — the lock is not an oracle for password validity
