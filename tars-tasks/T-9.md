# T-9: Per-account and per-IP limiter on /auth/login

Checkpoint by the TARS TDD orchestration for requirement REQ-42 (Login screen with first-party email/password auth).
Run: tdd-REQ-42-login-demo
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-22 — The 6th failed attempt for the same account within 15 minutes returns 429 AUTH_RATE_LIMITED with retry_after_seconds
- AC-23 — The 31st attempt from one IP within 15 minutes returns 429 regardless of account, so enumeration across accounts is also throttled
- AC-24 — A successful login resets the per-account counter
