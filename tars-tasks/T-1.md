# T-1: Users table: credential and lockout columns

Checkpoint by the TARS TDD orchestration for requirement REQ-42 (Login screen with first-party email/password auth).
Run: tdd-login-screen-with-first-party-email-pass-login-demo
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-1 — Migration adds password_hash, password_updated_at, failed_login_count, locked_until and applies then rolls back cleanly on a copy of the prod schema
- AC-2 — Existing rows migrate with password_hash NULL and are treated as 'no password set' by later code
