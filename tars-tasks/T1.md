# T1: Reset-token table + POST /auth/reset-password endpoint

Checkpoint by the TARS TDD orchestration for requirement REQ-PWRESET (Password reset flow).
Run: tdd-password-reset-flow-demo-1
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-1 — Migration applies and rolls back cleanly on a copy of the prod schema
- AC-2 — A valid, unexpired token sets the new password and 200s; a reused or expired token 400s
