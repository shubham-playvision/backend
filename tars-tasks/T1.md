# T1: Verification-token table + POST /auth/send-verification-email + POST /auth/verify-email endpoints

Checkpoint by the TARS TDD orchestration for requirement REQ-3 (Email Verification).
Run: tdd-email-verification-demo-1
Real code lands here once the test writer / implementer / validator components exist.

## Acceptance criteria
- AC-1 — Migration applies and rolls back cleanly on a copy of the prod schema
- AC-2 — A valid, unexpired token marks the account verified and 200s; a reused or expired token 400s
- AC-3 — Resending a verification email invalidates any previously issued token for that account
